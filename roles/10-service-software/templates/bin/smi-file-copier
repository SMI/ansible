#!/usr/bin/env bash

# {{ ansible_managed }}

set -euo pipefail

# shellcheck source=/dev/null
. _smi-load-env.bash

set -x
"{{ smi_service_software_install_dir }}/software/SmiServices/v${SMI_SMISERVICES_VERSION:?}/smi/smi" \
    file-copier \
    -y "{{ smi_service_software_install_dir }}/configs/${SMI_ENV:?}/file_extract_services.yaml"
